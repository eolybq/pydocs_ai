name: FRONTEND-Hugging Face Hub Deployment

on:
  push:
    branches: [main] 
    paths:
      - 'frontend/**'

  workflow_dispatch:

jobs:
  deploy-to-hf:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff pyright pandas-stubs
          pip install -r frontend/requirements.txt 

      - name: Ruff Check and Format
        run: |
          ruff check frontend/
          ruff format --check --diff frontend/

      - name: Pyright Type Check
        run: pyright frontend/

      - name: Push to hub
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git config --global user.email "bot@github.com"
          git config --global user.name "GitHub Actions Bot"

          cp frontend/metadata_hf.md frontend/README.md
          git add frontend/README.md
          git commit -m "Setup HF Metadata" || echo "No changes to commit"

          git remote add space "https://yezdata:$HF_TOKEN@huggingface.co/spaces/yezdata/pydocs-ai"

          git subtree split --prefix frontend -b hf-deploy-branch
          git push space hf-deploy-branch:main --force
          git branch -D hf-deploy-branch

